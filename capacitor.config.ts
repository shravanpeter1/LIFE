import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.life.app',
  appName: 'L.I.F.E',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
